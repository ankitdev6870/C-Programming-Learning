#include <stdio.h>

int main()
{
int l,b,Area;
printf("Enter length:",l);
scanf("%d",&l);
printf("Enter Breadth:",b);
scanf("%d",&b);
Area=l*b;
printf("The area of Rectangle is %d",Area);
    return 0;
}